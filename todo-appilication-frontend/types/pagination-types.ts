export type PaginationInfo = {
    currentPage: number;
    totalPages: number;
    totalResults: number;
    pageSize: number;
  };

  export type PaginationSliceState = {
    currentPage: number;
    totalPages: number;
    totalResults: number;
    pageSize: number;
  };